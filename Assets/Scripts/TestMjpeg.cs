using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using GitHub.secile.Avi;
using System.IO;
public class TestMjpeg : MonoBehaviour
{

	
    // Start is called before the first frame update
    void Start()
    {

	    //var path = "test.avi";
	    //var output = System.IO.File.OpenWrite(path);
	    //var writer = new MjpegWriter(output, 1920	, 1080, 30);

	    
	    
	    //for (int i = 1; i < 21; i++) {
	    //	byte[] imageData = File.ReadAllBytes("a"+i+".jpg");
	    //	 writer.AddImage(imageData);
	    //}
	    //// writer frame
	    ////foreach (var item in dialog.FileNames)
	    ////{
		////    writer.AddImage(new Bitmap(item));
	    ////}
	    //writer.Close();
	    //Debug.Log("DONE");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
